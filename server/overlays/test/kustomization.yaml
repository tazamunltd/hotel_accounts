resources: 
 - ../../base

namespace: test

namePrefix:
  test-

nameSuffix:
  -v1

patchesStrategicMerge:
- replicas.yaml
  

configMapGenerator:
- name: postgres-config
  env: config.properties
  namespace: test
